// import React from 'react';
// import Enzyme, {shallow} from 'enzyme';
// import Adapter from 'enzyme-adapter-react-16';

// Enzyme.configure({adapter: new Adapter() });

describe('FirstStep', () => {
    it('next button is disabled', () => {

    })

    it('next button is enabled when user fills in everything', () => {
        
    })

    it('user enters invalid input', () => {

    })

    it('user is redirected to flat sign up', () => {

    })

    it('user is redirected to flatee sign up', () => {

    })
});